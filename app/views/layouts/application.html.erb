<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Bros Nuzlocke Tracker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- ナビゲーションバー 🧭 -->
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top" style="background: var(--pokemon-gradient); backdrop-filter: blur(10px);">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand fw-bold" do %>
          🎮 Bros Nuzlocke Tracker
        <% end %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to dashboard_path, class: "nav-link #{'active' if current_page?(dashboard_path)}" do %>
                  📊 ダッシュボード
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to challenges_path, class: "nav-link #{'active' if current_page?(challenges_path)}" do %>
                  🎯 チャレンジ
                <% end %>
              </li>
            <% end %>
            <li class="nav-item">
              <%= link_to pokedex_index_path, class: "nav-link #{'active' if current_page?(pokedex_index_path) || current_page?(controller: 'pokedex')}" do %>
                📖 ポケモン図鑑
              <% end %>
            </li>
          </ul>
          
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                  👤 <%= current_user.display_name %>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "⚙️ 設定", edit_user_registration_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "🚪 ログアウト", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "🚪 ログイン", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "📝 新規登録", new_user_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- 通知・アラート 📢 -->
    <div class="container-fluid">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show mx-3 mt-3" role="alert" style="border-radius: 12px; animation: fadeInScale 0.5s ease-out;">
          <i class="me-2">✅</i><%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show mx-3 mt-3" role="alert" style="border-radius: 12px; animation: fadeInScale 0.5s ease-out;">
          <i class="me-2">⚠️</i><%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    </div>

    <!-- メインコンテンツ -->
    <div class="page-content">
      <%= yield %>
    </div>

    <!-- フッター 🌟 -->
    <footer class="mt-5 py-4" style="background: var(--pokemon-gradient); color: white;">
      <div class="container text-center">
        <p class="mb-0">
          <small>
            🎮 Bros Nuzlocke Tracker - ポケモンナズロックの冒険を記録しよう！ ✨<br>
            Made with 💖 for Pokemon trainers everywhere
          </small>
        </p>
      </div>
    </footer>
  </body>
</html>
