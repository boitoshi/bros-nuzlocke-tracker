<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card shadow">
      <div class="card-body">
        <h2 class="card-title text-center mb-4">新規登録</h2>
        
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "needs-validation", novalidate: true }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>
          
          <div class="mb-3">
            <%= f.label :username, "ユーザー名 *", class: "form-label" %>
            <%= f.text_field :username, class: "form-control", placeholder: "3-20文字、英数字とアンダースコア(_)のみ", required: true %>
            <div class="form-text text-muted">
              3〜20文字、英数字とアンダースコア(_)のみ使用可能
            </div>
          </div>
          
          <div class="mb-3">
            <%= f.label :email, "メールアドレス（任意）", class: "form-label" %>
            <%= f.email_field :email, class: "form-control", placeholder: "example@email.com", autocomplete: "email" %>
            <div class="form-text text-muted">
              パスワードリセット機能を使う場合は必要です
            </div>
          </div>
          
          <div class="mb-3">
            <%= f.label :password, "パスワード *", class: "form-label" %>
            <%= f.password_field :password, class: "form-control", autocomplete: "new-password", required: true %>
            <div class="form-text text-muted">
              <strong>パスワード要件：</strong><br>
              • 6文字以上<br>
              • 英数字を含む<br>
              • 大文字・小文字・数字・記号を組み合わせることを推奨
            </div>
          </div>
          
          <div class="mb-3">
            <%= f.label :password_confirmation, "パスワード確認 *", class: "form-label" %>
            <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "new-password", required: true %>
          </div>
          
          <div class="d-grid">
            <%= f.submit "アカウント作成", class: "btn btn-primary btn-lg" %>
          </div>
        <% end %>
        
        <div class="text-center mt-3">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>
